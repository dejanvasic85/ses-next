# SES Melbourne — Fix List (May 2026)

## 🔴 Critical

- [x] **UTM canonical not working** — `/?utm_campaign=gmb` is still indexed as a separate page with 61 clicks. Canonical tag either didn't ship or isn't stripping the query param. Fix: ensure every page renders `<link rel="canonical" href="https://www.sesmelbourne.com.au{router.asPath.split('?')[0]}" />` and verify via `view-source:` on the deployed URL.

- [ ] **No form submission tracking** — GA4 only records `form_start`, not actual submissions. Need a `generate_lead` event firing on successful form submit before ads can run. Wire up in GTM or directly in the contact form component.

- [ ] **Commercial Electrician + Emergency Electrician pages missing** — Both appear as unlinked text in the footer. Either the pages don't exist or they're returning 404s. Create and publish both pages in Sanity, then link them properly in the footer nav.

---

## 🟠 High

- [ ] **Identify and fix 3 x 404 errors** — Go to Search Console > Pages > Not Found (404) and list the exact URLs. Likely the two service pages above plus one other. Either create the pages or 301 redirect to the closest match.

- [ ] **8 pages "crawled but not indexed"** — Google flagged these as too thin to index. Go to Search Console > Pages > Crawled - currently not indexed. Identify which pages and add more substantive content (aim for 600+ words with FAQs and internal links).

- [ ] **15 pages "discovered but not indexed"** — Mostly new location pages sitting in the queue. Submit each one individually via URL Inspection tool in Search Console to speed up indexing.

- [ ] **Review snippets author warning** — 99 items flagged for "invalid object type for field author." The `author` field on blog/review schema is likely a plain string instead of a `Person` object. Fix: `"author": { "@type": "Person", "name": "Karl Rainbow" }`.

- [ ] **"electrician altona" CTR is 0.1%** — 810 impressions, 1 click. Location page exists but position is still poor. Bulk up the Altona page content — more local detail, FAQs with schema, internal links from service pages.

---

## 🟡 Medium

- [ ] **Homepage title says "24/7"** — Karl doesn't do after-hours. Update title to use "Same-Day Priority Response" to match the rest of the site copy. Fix in Sanity or `_app.tsx` / `index.tsx`.

- [ ] **`/locations` meta description says "Solar Energy Solutions"** — Wrong company name, old copy. Update in Sanity to something like: "Storm Electrical Solutions services Melbourne's western suburbs. Find your local electrician in Altona, Footscray, Newport, Yarraville and more."

---

## Notes

- Do not run paid ads until form submission tracking is confirmed working end-to-end.
- After fixing 404s, resubmit sitemap in Search Console.
- After fixing author schema, revalidate at https://search.google.com/test/rich-results
